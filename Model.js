function maximizeGreenLetters() {
    return
}

/**
 * A pattern for two words represents the wordle-similarity 
 * pattern (grey -> 0, yellow -> 1, green -> 2) but as an integer
 * between 0 and 3^5. Reading this integer in ternary gives the\
 * associated pattern.
 * This function computes the pairwise patterns between two lists 
 * of words, returning the result as a grid of hash values. 
 * @param {*} all_words 
 * @param {*} possible_words 
 * @returns 
 */
function getPatternMatrix(all_words, possible_words) {

    return
}

function getPatternDistributions(patternMatrix, all_words, possible_words) {
}

function getEntropy(all_words, possible_words) {
    const patternMatrix = getPatternMatrix(all_words, possible_words)
    return get_patter 
}

/** Maximizing the number of new yellow letters using information gain */
function maximizeYellowLetters() {
    return
}

/** Decide which of the maximizeGreenLetters or maximizeYellowLetters to use at different stages of the game */
function strategyDecider() {
    return
}